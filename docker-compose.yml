services:
  jira-dashboard:
    build: .
    ports:
      - "3456:3456"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/cache.db
    volumes:
      - cache-data:/app/data
    restart: unless-stopped

volumes:
  cache-data:
